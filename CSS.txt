:root {
    --cor-verde-oficial: #19A32A;
    --cor-amarelo-oficial: #FFDE00;
    --cor-azul-oficial: #00509F;
    --cor-branco: #FFFFFF;
    --cor-cinza-claro: #f4f4f9;
    --cor-cinza-texto: #333;
    --cor-cinza-borda: #ddd;
    --font-principal: 'Inter', Arial, sans-serif;
    --sombra-padrao: 0 2px 5px rgba(0,0,0,0.1);
}

body {
    font-family: var(--font-principal);
    background-color: var(--cor-cinza-claro);
    color: var(--cor-cinza-texto);
    margin: 0;
    line-height: 1.6;
}

.container { max-width: 1400px; margin: 20px auto; padding: 0 20px; }

.header-main {
    background-color: var(--cor-azul-oficial);
    color: var(--cor-branco);
    padding: 15px 30px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    box-shadow: var(--sombra-padrao);
}

.header-main .header-logo img { height: 80px; width: auto; }
.header-main .header-title { text-align: center; flex-grow: 1; margin: 10px 20px; }
.header-main h1, .header-main h2, .header-main h3 { margin: 0; font-weight: normal; }
.header-main h1 { font-size: 1em; }
.header-main h2 { font-size: 0.9em; opacity: 0.9; }
.header-main h3 { font-size: 1.2em; font-weight: bold; margin-top: 5px; }
.header-main .header-status { font-size: 0.8em; text-align: right; min-width: 180px; }

.card {
    background-color: var(--cor-branco);
    border-radius: 8px;
    box-shadow: var(--sombra-padrao);
    padding: 20px;
    margin-bottom: 20px;
}

.grid-container {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 20px;
}

.grid-container .card h4 { margin-top: 0; color: var(--cor-azul-oficial); }
.grid-container .card p { font-size: 2em; font-weight: bold; margin: 10px 0 0 0; color: #333; }
.footer-main { text-align: center; padding: 20px; font-size: 0.9em; color: #888; }
